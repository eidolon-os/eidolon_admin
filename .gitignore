# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/
env/

# Node / Vite
web/node_modules/
web/dist/
web/.vite/
.npm/
.pnpm-store/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Runtime artifacts (supervisord pid/socket/childlogs)
var/

# Benchmark artifacts
benchmarks/runs/
benchmark/runs/
reports/benchmark*/
reports/*benchmark*/
reports/*_benchmark_*/
reports/memory_perf_*/
reports/memory_quality_*/
reports/memory_multi_device_*/
reports/e2e_*/

# Supervisord enabled/ is RUNTIME STATE, not source: the Admin UI's
# Enable/Disable buttons mutate this directory, and any git operation
# that touched these symlinks would silently override the user's
# choices. The set of "default-enabled" services lives in
# ``config/default-enabled.txt``; run_all.sh's seed_enabled_symlinks
# materializes the directory on first start (sentinel: ``enabled/.seeded``).
deploy/supervisor/enabled/*
!deploy/supervisor/enabled/.gitkeep

# Local override for service registry (if you ever do per-machine tweaks)
config/services.local.yaml
config/esp32_tools.local.yaml

# Plans, ad-hoc notes
plans/
*.local.md

# Local trash from safe-delete flows
**/.trash/

# Editor-generated
*.tmp
*.bak

# Local OS/editor/runtime hygiene
**/.DS_Store
._*
**/._*
.AppleDouble/
.LSOverride
.Spotlight-V100/
.Trashes/
.fseventsd/
Thumbs.db
desktop.ini
*.swo
*~
.claude/
.import_linter_cache/
logs/
*.log
*.pid
*.sock
*.sqlite3-wal
*.sqlite3-shm
*.db-wal
*.db-shm
